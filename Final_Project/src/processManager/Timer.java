package processManager;

import processManager.InterruptHandler.Interrupt;

public class Timer extends Thread {
	public Timer(Interrupt interrupt) { //타이머 인터럽트 / IHR이 얘를 불러야 함
		
	}
	
	public void run() {
		
	}
}
